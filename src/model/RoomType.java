package model;

//This is an enumeration. which means that the user is provided with only 2 options to choose from. and there is no chance of outside altercation
public enum RoomType {
    SINGLE,
    DOUBLE
}
